# RLD (Reflective Latent Draft) 依赖
# 大部分依赖与 ColaMem_lora 共享

torch>=2.1.0
transformers==5.3.0
accelerate>=1.0.0
deepspeed>=0.14.0
peft>=0.10.0
flash_attn>=2.5.0

# 数据处理
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0

# Qwen3-VL 相关
qwen-vl-utils>=0.0.8

# 预生成推理链加速 (scripts/pregenerate_think.py)
vllm>=0.6.0

# iChat API 网关 (Stage 2 错误定位, 通过公司内部网关调用 Gemini)
requests>=2.28.0

# 配置
PyYAML>=6.0

# 日志
tensorboard>=2.14.0

# 工具
tqdm>=4.65.0
